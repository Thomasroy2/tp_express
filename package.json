{
  "name": "tp",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "dev": "nodemon app.js",
    "test": "istanbul cover ./node_modules/mocha/bin/_mocha ./test/**/*.spec.js --report lcovonly -- -R spec && codecov",
    "test:watch": "istanbul cover ./node_modules/mocha/bin/_mocha ./test/**/*.spec.js --report lcovonly -- -R spec && codecov"
  },
  "config": {
    "blanket": {
      "pattern": "./controllers"
    }
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.18.2",
    "express": "^4.16.2",
    "istanbul": "^0.4.5",
    "lodash": "^4.17.4"
  },
  "devDependencies": {
    "chai": "^4.1.2",
    "mocha": "^4.0.1",
    "sinon": "^4.1.2",
    "supertest": "^3.0.0"
  }
}
